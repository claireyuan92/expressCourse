public class Solution {

    // haw to represent a graph programmally: hashtable: key , value(neigbors): set, linkedlist(neighbors)
    public boolean canFinish(int numCourses, int[][] prerequisites) {
	HashMap<int, Set<int>> adjacentList;
    }
}
